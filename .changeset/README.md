# Changesets

This directory holds changesets, which describe pending version bumps and
changelog entries for the packages in this repository.

To record a change, run `pnpm changeset` from the repository root and
follow the prompts. Commit the generated file alongside your change.

See https://github.com/changesets/changesets for full documentation.
