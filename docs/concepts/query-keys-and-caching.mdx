---
title: "Query key and cache invalidation model"
description: "How Sorokit builds TanStack Query keys, and how cache invalidation stays consistent"
---

Every hook in Sorokit that touches the network is a TanStack Query hook, and every one of them builds its query key from `@sorokit/core`'s `queryKeys` object rather than constructing a key array by hand. This is the single source of truth for how Sorokit's cache is shaped, so calls, invalidation, and the devtools cache viewer never drift out of sync with each other.

```ts
import { queryKeys } from "@sorokit/core";

queryKeys.account(address);
queryKeys.balance(address, assetId);
queryKeys.transactionStatus(hash);
queryKeys.networkStatus();
queryKeys.contractSpec(networkPassphrase, contractId);
queryKeys.contractCall(networkPassphrase, contractId, method, args);
queryKeys.contractCallsByContract(networkPassphrase, contractId);
```

Every key is namespaced under `"sorokit"`, so Sorokit's cache entries are trivially distinguishable from your app's own TanStack Query usage in the devtools cache viewer.

## Invalidation after a send

`queryKeys.contractCallsByContract(networkPassphrase, contractId)` returns the prefix shared by every `contractCall` query for a given contract. `useContractSend` passes this prefix to `queryClient.invalidateQueries` on success, which invalidates every call for that contract regardless of which methods or args have been queried, without needing to know them in advance:

```ts
queryClient.invalidateQueries({
  queryKey: queryKeys.contractCallsByContract(networkPassphrase, contractId),
});
```

## Args containing bigint or bytes

Contract args routinely contain `bigint` (any `u64`/`u128`/`u256`-typed argument) and `Uint8Array` (bytes arguments). TanStack Query hashes query keys with `JSON.stringify`, which throws on a raw `bigint`. `queryKeys.contractCall` handles this for you: it recursively converts `bigint` to a tagged string and `Uint8Array` / `Map` values to JSON-safe forms before building the key array. This only affects the cache key; the actual values passed to the contract call are untouched.

## Spec caching

`queryKeys.contractSpec(networkPassphrase, contractId)` is cached with an infinite `staleTime`, since a deployed contract's spec cannot change without redeploying under a new contract ID. This is what makes Sorokit's no-codegen model work: the first hook call for a contract pays the cost of fetching and deriving its schema, and every later call, anywhere in your app, reuses the cached result.

## Reusing a QueryClient

If your app already renders its own `QueryClientProvider`, pass that client's instance to `SorokitProvider`'s `queryClient` prop instead of letting it create a second one:

```tsx
<SorokitProvider network="testnet" queryClient={myExistingQueryClient}>
```
