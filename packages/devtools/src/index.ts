/**
 * @soroform/devtools is not yet implemented.
 *
 * This placeholder exists so the package builds, lints, typechecks, and
 * tests cleanly as part of the monorepo's tooling scaffold.
 * SoroformDevtools lands here in a later milestone.
 */
export const SOROFORM_DEVTOOLS_VERSION = "0.0.0";
