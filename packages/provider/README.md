# @soroform/provider

React context provider for Soroform. Wraps
`@tanstack/react-query`'s `QueryClientProvider` and supplies a
`SoroformConfig` (network, RPC URL, Horizon URL) to the rest of the
React tree.

Full documentation: https://docs.soroform.dev (see the repository root
README for the current status of the docs site).
