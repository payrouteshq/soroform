/**
 * @soroform/provider is not yet implemented.
 *
 * This placeholder exists so the package builds, lints, typechecks, and
 * tests cleanly as part of the monorepo's tooling scaffold.
 * SoroformProvider and useSoroformConfig land here in a later milestone.
 */
export const SOROFORM_PROVIDER_VERSION = "0.0.0";
