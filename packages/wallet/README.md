# @soroform/wallet

Wallet connection and signing layer for Soroform. Provides
`WalletProvider`, `useWallet`, and a minimally styled
`ConnectWalletButton`, built on top of a multi-wallet Stellar connector.

Full documentation: https://docs.soroform.dev (see the repository root
README for the current status of the docs site).
